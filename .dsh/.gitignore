# DSH harness delegation state: one dlg-* directory per delegation run
# (task/progress/resume logs), generated by the delegation protocol — never
# part of the source tree. The dirs tracked historically were untracked with
# `git rm -r --cached`; this rule keeps future runs out of git too.
delegations/